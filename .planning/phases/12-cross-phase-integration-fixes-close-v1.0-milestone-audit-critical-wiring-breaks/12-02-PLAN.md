---
phase: 12-cross-phase-integration-fixes
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
gap_closure: true
files_modified:
  - apps/frontend/src/features/clean/CleanStep.tsx
autonomous: true
requirements:
  - FR4

must_haves:
  truths:
    - "handleCellReconciled called with outcome===null sends clear_reconciliation:true in the PATCH body — No-match clears now persist to checkpoint.json"
    - "handleCellReconciled called with a non-null ReconciliationOutcome sends reconciliation:outcome in the PATCH body (the existing non-null path is correct and must not change)"
    - "clear_reconciliation:true is the canonical clear signal per Phase 11 convention — reconciliation:null is never sent; JSON.stringify would drop undefined, not null, but the conditional spread avoids both"
    - "The audit_entry key in the PATCH body is unchanged — Fix 2 only changes the reconciliation/clear_reconciliation part of the payload"
  artifacts:
    - path: "apps/frontend/src/features/clean/CleanStep.tsx"
      provides: "handleCellReconciled with conditional spread: outcome===null → clear_reconciliation:true; outcome non-null → reconciliation:outcome"
      contains: "clear_reconciliation"
  key_links:
    - from: "apps/frontend/src/features/clean/CleanStep.tsx"
      to: "apps/backend/app/api/api_v1/endpoints/batches.py"
      via: "patchResult sends clear_reconciliation:true → backend clears row validation[field].reconciliation in checkpoint.json"
      pattern: "clear_reconciliation"
---

<objective>
Fix 2: CleanStep.handleCellReconciled PATCH payload — when outcome===null (No-match), use conditional spread to send clear_reconciliation:true instead of relying on outcome??undefined which JSON.stringify silently drops.

Purpose: This one-line conditional spread closes the FR4 bug where "No-match" clears were applied to Zustand (UI looked right) but never persisted to checkpoint.json. After reload, the stale reconciliation reappeared.
Output: CleanStep.tsx handleCellReconciled with corrected PATCH payload construction.
</objective>

<execution_context>
@/Users/zu54tav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zu54tav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-cross-phase-integration-fixes-close-v1.0-milestone-audit-critical-wiring-breaks/12-RESEARCH.md
@.planning/phases/11-authority-reconciliation-per-field-reconciliation-against-gnd-wikidata-geonames-getty-aat-with-candidate-picker-bulk-column-mode-cache-and-authority-uri-emission-in-lido-marcxml-dublin-core-exports/11-04-SUMMARY.md

@apps/frontend/src/features/clean/CleanStep.tsx
@apps/frontend/src/api/batchesApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix handleCellReconciled null path — conditional spread for clear_reconciliation</name>
  <files>
    apps/frontend/src/features/clean/CleanStep.tsx
  </files>
  <action>
    Read `apps/frontend/src/features/clean/CleanStep.tsx`. Locate the `handleCellReconciled` function (research confirms it is at approximately lines 574–617). Find the `patchResult(...)` call inside it.

    The CURRENT (buggy) PATCH payload construction looks like:
    ```ts
    patchResult(batchId, pageFilename, {
      field,
      reconciliation: outcome ?? undefined,   // BUG: outcome===null → undefined → JSON.stringify drops it
      audit_entry: auditEntry,
    }).catch(err => console.warn('[CleanStep] reconcile PATCH failed', err));
    ```

    Replace ONLY the `reconciliation: outcome ?? undefined` line with a conditional spread. The replacement is a targeted surgical edit — do not change any other code in handleCellReconciled:

    ```ts
    patchResult(batchId, pageFilename, {
      field,
      ...(outcome === null
        ? { clear_reconciliation: true }
        : { reconciliation: outcome }),
      audit_entry: auditEntry,
    }).catch(err => console.warn('[CleanStep] reconcile PATCH failed', err));
    ```

    PITFALL — Scope of this fix:
    - This fix ONLY handles the null/non-null path for the reconciliation/clear_reconciliation keys.
    - Do NOT touch the `audit_entry` key — it is already correct.
    - Do NOT touch the Zustand state update block above the patchResult call — it is already correct.
    - Do NOT change any other function in CleanStep.tsx. This is a one-location edit within handleCellReconciled.

    PITFALL — audit_entry handling:
    Per the STATE.md key decision "audit_entry sent ONCE per bulk operation" — the existing audit_entry pattern in handleCellReconciled is correct as-is. Fix 2 must NOT change how audit_entry is constructed or conditionally sent. Only the reconciliation payload changes.

    PITFALL — why conditional spread and not reconciliation: outcome ?? null:
    `patchResult` signature has `reconciliation?: ReconciliationOutcome` (not nullable with null value). Sending `reconciliation: null` would be a type error. The backend uses `clear_reconciliation: bool = False` as the unambiguous "clear" signal (Phase 11 Plan 01 STATE.md decision). The conditional spread correctly routes: null outcome → clear_reconciliation:true; non-null outcome → reconciliation:outcome.

    After editing, confirm `clear_reconciliation` is referenced inside the handleCellReconciled function body (not just in the handleApplyTransform or executeClusterApply paths that Phase 11 Plan 04 already fixed).
  </action>
  <verify>
    <automated>grep clear_reconciliation /Users/zu54tav/workspace/Indexcards_OCR/apps/frontend/src/features/clean/CleanStep.tsx</automated>
    <manual>Grep output must show an occurrence of clear_reconciliation inside the handleCellReconciled function body (search context: look for lines near 'reconcile PATCH failed' warning). Also run TypeScript check: cd /Users/zu54tav/workspace/Indexcards_OCR/apps/frontend && npx tsc --noEmit 2>&1 | grep -v node_modules | grep "error TS" | head -10 || echo "tsc OK"</manual>
    <sampling_rate>run after this task commits — Wave 2 Plan 02 complete</sampling_rate>
  </verify>
  <done>
    handleCellReconciled sends clear_reconciliation:true when outcome===null, and reconciliation:outcome when outcome is non-null. The buggy `reconciliation: outcome ?? undefined` line is replaced with a conditional spread. audit_entry handling unchanged. tsc passes cleanly.
  </done>
</task>

</tasks>

<verification>
- `grep clear_reconciliation apps/frontend/src/features/clean/CleanStep.tsx` returns a match inside handleCellReconciled (look for context near "reconcile PATCH failed").
- The `reconciliation: outcome ?? undefined` pattern no longer appears in CleanStep.tsx.
- `cd apps/frontend && npx tsc --noEmit` produces no new errors.
</verification>

<success_criteria>
- No-match clicks in CandidateDrawer persist the clear to checkpoint.json — reloading the batch no longer restores a previously-cleared reconciliation.
- Non-null outcome paths continue to send reconciliation:outcome unchanged.
- TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/12-cross-phase-integration-fixes-close-v1.0-milestone-audit-critical-wiring-breaks/12-02-SUMMARY.md`
</output>
