---
phase: 12-cross-phase-integration-fixes
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - apps/backend/app/services/template_service.py
  - packages/shared-types/schemas/batch.schema.json
  - apps/backend/app/models/schemas.py
autonomous: true
requirements:
  - FR2
  - FR5

must_haves:
  truths:
    - "Saving a template that includes authority_bindings persists the bindings to disk — reloading the template restores all field authority configurations"
    - "update_template with authority_bindings uses the same is-not-None guard as the existing name/fields/prompt_template/field_rules pattern — omitting authority_bindings in a PATCH leaves the stored value unchanged"
    - "authority_bindings values in update_template are serialized to plain dicts using the same v.dict() if hasattr(v,'dict') else v pattern as create_batch in batches.py"
    - "ExtractionResult in batch.schema.json contains an edited_data property (Dict[str,str] | null, default null)"
    - "ExtractionResult Pydantic model in schemas.py contains edited_data: Optional[Dict[str, str]] = None"
    - "turbo generate runs cleanly after the JSON Schema change and edited_data appears in packages/shared-types/generated/ts/index.ts"
  artifacts:
    - path: "apps/backend/app/services/template_service.py"
      provides: "authority_bindings forwarding in create_template() constructor and update_template() is-not-None guard"
      contains: "authority_bindings"
    - path: "packages/shared-types/schemas/batch.schema.json"
      provides: "edited_data property inside ExtractionResult definition"
      contains: "edited_data"
    - path: "apps/backend/app/models/schemas.py"
      provides: "edited_data field on ExtractionResult Pydantic model"
      contains: "edited_data"
    - path: "packages/shared-types/generated/ts/index.ts"
      provides: "edited_data field visible in generated TypeScript output"
      contains: "edited_data"
  key_links:
    - from: "apps/backend/app/services/template_service.py"
      to: "apps/backend/app/models/schemas.py"
      via: "TemplateCreate.authority_bindings / TemplateUpdate.authority_bindings passed through service layer"
      pattern: "authority_bindings"
    - from: "packages/shared-types/schemas/batch.schema.json"
      to: "packages/shared-types/generated/ts/index.ts"
      via: "turbo generate codegen pipeline"
      pattern: "edited_data"
---

<objective>
Backend foundation fixes: forward authority_bindings through template_service (Fix 1 — FR2 gap) and add edited_data to ExtractionResult in JSON Schema + Pydantic (Fix 4a/4b — FR5 schema foundation).

Purpose: Fix 1 closes the identical regression pattern that was caught and fixed for prompt_template in Phase 03.1 Plan 03 — now applied to authority_bindings. Fix 4a/4b adds the schema field that Wave 2 Plan 12-04 will use for frontend hydration.
Output: template_service.py with authority_bindings forwarding; batch.schema.json + schemas.py with edited_data; regenerated codegen output.
</objective>

<execution_context>
@/Users/zu54tav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zu54tav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-cross-phase-integration-fixes-close-v1.0-milestone-audit-critical-wiring-breaks/12-RESEARCH.md
@.planning/phases/03.1-dynamic-prompt-generation-from-field-definitions-with-configurable-prompt-template/03.1-03-SUMMARY.md

@apps/backend/app/services/template_service.py
@packages/shared-types/schemas/batch.schema.json
@apps/backend/app/models/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 1 — template_service forwards authority_bindings in create_template and update_template</name>
  <files>
    apps/backend/app/services/template_service.py
  </files>
  <action>
    Read `apps/backend/app/services/template_service.py` in full before editing.

    IDENTIFY the following in the current file:
    - `create_template()`: the `Template(...)` constructor call (currently passes id, name, fields, prompt_template, field_rules — authority_bindings is missing)
    - `update_template()`: the sequence of `if template_in.X is not None:` guards (has name, fields, prompt_template, field_rules — authority_bindings guard is missing)

    **Pitfall 1 — create_template() constructor kwarg:**
    Add `authority_bindings=template_in.authority_bindings` to the `Template(...)` constructor call, alongside the existing `field_rules=template_in.field_rules` kwarg. The `Template` Pydantic model handles serialization when `.dict()` is called on the new_template object (line after construction). No manual serialization needed in create_template().

    ```python
    new_template = Template(
        id=str(uuid.uuid4()),
        name=template_in.name,
        fields=template_in.fields,
        prompt_template=template_in.prompt_template,
        field_rules=template_in.field_rules,
        authority_bindings=template_in.authority_bindings,  # ADD THIS — Phase 12 Fix 1
    )
    ```

    **Pitfall 2 — update_template() is-not-None guard:**
    Add the guard AFTER the existing `field_rules` branch (not before — maintain the same positional order as the create_template constructor). Use the SAME is-not-None guard pattern as the existing name/fields/prompt_template/field_rules branches. Do NOT clobber existing authority_bindings when the PATCH request omits the field.

    The authority_bindings values arrive as `AuthorityBinding` Pydantic model instances (parsed by FastAPI). They MUST be serialized to plain dicts before writing to the JSON store. Apply the same `v.dict() if hasattr(v, 'dict') else v` pattern that `create_batch()` in `apps/backend/app/api/api_v1/endpoints/batches.py` uses for authority_bindings serialization:

    ```python
    if template_in.authority_bindings is not None:                    # ADD THIS BLOCK — Phase 12 Fix 1
        templates[i]["authority_bindings"] = {
            k: (v.dict() if hasattr(v, "dict") else v)
            for k, v in template_in.authority_bindings.items()
        }
    ```

    Insert this block immediately after the `if template_in.field_rules is not None:` block in update_template().

    CRITICAL: Do NOT change any other code in template_service.py. The existing name/fields/prompt_template/field_rules branches must remain exactly as they are. This is a targeted two-location addition only.
  </action>
  <verify>
    <automated>grep authority_bindings /Users/zu54tav/workspace/Indexcards_OCR/apps/backend/app/services/template_service.py</automated>
    <manual>Grep output must show at least 2 matches — one in create_template (constructor kwarg) and one in update_template (is-not-None guard). Also verify no syntax errors: cd /Users/zu54tav/workspace/Indexcards_OCR/apps/backend && python -c "from app.services.template_service import template_service; print('import ok')"</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    template_service.py has authority_bindings in create_template() constructor and update_template() guard. Save-then-reload cycle no longer silently drops authority bindings. Python imports cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix 4a/4b — add edited_data to JSON Schema ExtractionResult + Pydantic model + regenerate codegen</name>
  <files>
    packages/shared-types/schemas/batch.schema.json
    apps/backend/app/models/schemas.py
  </files>
  <action>
    **Step A — packages/shared-types/schemas/batch.schema.json:**

    Read the full file. Find the `ExtractionResult` definition inside the `$defs` block. It has a `properties` object with fields: filename, batch, success, data, error, duration, validation. Locate the `validation` property block (the last property before the closing `}` of `ExtractionResult.properties`).

    Insert the following property AFTER `validation` and BEFORE the closing `}` of `properties`:

    ```json
    "edited_data": {
      "anyOf": [
        {
          "additionalProperties": { "type": "string" },
          "type": "object"
        },
        { "type": "null" }
      ],
      "default": null,
      "title": "Edited Data",
      "description": "Curator field edits persisted by PATCH endpoint. Dict[field_name, edited_value]. Phase 9 PATCH writes here; Phase 12 adds round-trip read."
    }
    ```

    CRITICAL SCOPE NOTE: This is the ONLY change to batch.schema.json in Phase 12. Do NOT add AuditEntry, ResultPatch, AuthorityBinding, or ReconciliationOutcome to the JSON Schema — that is Phase 13 work. Keep the scope tight to edited_data on ExtractionResult only.

    **Step B — apps/backend/app/models/schemas.py:**

    Read the file. Find the `ExtractionResult` Pydantic model. It currently ends with:
    ```python
    validation: Optional[Dict[str, ValidationOutcome]] = None
    ```
    Add after this line:
    ```python
    edited_data: Optional[Dict[str, str]] = None  # Phase 9 PATCH writes curator edits; Phase 12 adds round-trip read
    ```

    This is additive — no existing fields change. The `Optional` with `None` default means existing checkpoint.json rows without edited_data deserialize cleanly (backward compat).

    **Step C — Run codegen:**

    From the repo root:
    ```bash
    cd /Users/zu54tav/workspace/Indexcards_OCR && npx turbo generate
    ```

    After running, verify `packages/shared-types/generated/ts/index.ts` contains `edited_data`. The generated TypeScript field should appear in the `ExtractionResult` interface. Note: No app code imports from generated/ yet (that is Phase 13 pipeline re-adoption work). The verification here is forward-looking: confirm the codegen pipeline accepts the new field.

    PITFALL — Scope boundary: Do NOT edit `apps/frontend/src/store/wizardStore.ts` in this task. The wizardStore.ts ExtractionResult TypeScript interface update belongs to Plan 12-04 (Wave 2). Plan 12-01 only handles the JSON Schema + Pydantic + codegen regeneration.
  </action>
  <verify>
    <automated>grep edited_data /Users/zu54tav/workspace/Indexcards_OCR/packages/shared-types/schemas/batch.schema.json && grep edited_data /Users/zu54tav/workspace/Indexcards_OCR/apps/backend/app/models/schemas.py && grep edited_data /Users/zu54tav/workspace/Indexcards_OCR/packages/shared-types/generated/ts/index.ts</automated>
    <manual>All three greps must return at least one match. Also confirm the JSON Schema is valid JSON: python3 -c "import json; json.load(open('packages/shared-types/schemas/batch.schema.json')); print('valid JSON')" from repo root. Confirm no AuditEntry/ResultPatch/AuthorityBinding/ReconciliationOutcome was accidentally added to JSON Schema (out of scope for Phase 12).</manual>
    <sampling_rate>run after this task commits — Wave 1 complete</sampling_rate>
  </verify>
  <done>
    edited_data property present in batch.schema.json ExtractionResult. edited_data field present in schemas.py ExtractionResult Pydantic model. turbo generate ran cleanly and edited_data appears in packages/shared-types/generated/ts/index.ts. No Phase 13 types accidentally added to JSON Schema.
  </done>
</task>

</tasks>

<verification>
- `grep authority_bindings apps/backend/app/services/template_service.py` returns at least 2 lines (create_template constructor + update_template guard).
- `grep edited_data packages/shared-types/schemas/batch.schema.json` returns a match inside ExtractionResult.properties.
- `grep edited_data apps/backend/app/models/schemas.py` returns a match on ExtractionResult.
- `grep edited_data packages/shared-types/generated/ts/index.ts` returns a match (codegen regenerated).
- `python -c "from app.services.template_service import template_service; print('ok')"` from apps/backend exits 0.
- `python3 -c "import json; json.load(open('packages/shared-types/schemas/batch.schema.json')); print('ok')"` from repo root exits 0.
</verification>

<success_criteria>
- Fix 1: authority_bindings round-trips through template save/load (save → reload → load template → bindings present in ConfigureStep).
- Fix 4a/4b: edited_data is a typed field in both JSON Schema and Pydantic ExtractionResult.
- Codegen pipeline accepts the new field and generates it in TypeScript output.
- No Phase 13 scope (AuditEntry/ResultPatch/AuthorityBinding/ReconciliationOutcome) in JSON Schema.
- Backend Python imports cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/12-cross-phase-integration-fixes-close-v1.0-milestone-audit-critical-wiring-breaks/12-01-SUMMARY.md`
</output>
