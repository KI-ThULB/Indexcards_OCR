---
phase: 08-validation-rules-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/schemas/template.schema.json
  - packages/shared-types/schemas/batch.schema.json
  - packages/shared-types/generated/ts/index.ts
  - packages/shared-types/generated/py/template.py
  - packages/shared-types/generated/py/batch.py
  - apps/backend/app/models/schemas.py
  - apps/frontend/src/api/batchesApi.ts
  - apps/frontend/src/api/templatesApi.ts
autonomous: true
requirements:
  - FR2
  - FR4

must_haves:
  truths:
    - "FieldRule and ValidationOutcome shapes exist in shared schema and codegen output"
    - "Pydantic schemas.py exposes FieldRule, ValidationOutcome, BatchCreate.field_rules/corrector_enabled/corrector_cap, ExtractionResult.validation, Template.field_rules"
    - "Frontend BatchCreate type accepts field_rules, corrector_enabled, corrector_cap"
    - "Existing batches without field_rules deserialize without error (null-default backward compatibility)"
  artifacts:
    - path: "packages/shared-types/schemas/template.schema.json"
      provides: "FieldRule definition, Template.field_rules property"
      contains: "FieldRule"
    - path: "packages/shared-types/schemas/batch.schema.json"
      provides: "ValidationOutcome definition, BatchCreate/BatchConfig.field_rules/corrector_*, ExtractionResult.validation"
      contains: "ValidationOutcome"
    - path: "packages/shared-types/generated/ts/index.ts"
      provides: "Generated TS types regenerated by turbo generate"
      contains: "FieldRule"
    - path: "apps/backend/app/models/schemas.py"
      provides: "Pydantic FieldRule, ValidationOutcome, extended BatchCreate/BatchConfig/ExtractionResult/Template"
      contains: "class FieldRule"
    - path: "apps/frontend/src/api/batchesApi.ts"
      provides: "Local BatchCreate interface extended with field_rules/corrector_*"
      contains: "field_rules"
  key_links:
    - from: "packages/shared-types/schemas/*.schema.json"
      to: "packages/shared-types/generated/ts/index.ts"
      via: "turbo generate (codegen step)"
      pattern: "FieldRule|ValidationOutcome"
    - from: "apps/backend/app/models/schemas.py"
      to: "packages/shared-types/schemas/batch.schema.json"
      via: "manual mirror — researcher confirmed schemas.py is hand-maintained"
      pattern: "field_rules|validation"
---

<objective>
Add the FieldRule and ValidationOutcome data shapes to the shared JSON schema and propagate them through the codegen pipeline (TypeScript + Pydantic), then mirror them by hand in the authoritative Pydantic file `apps/backend/app/models/schemas.py` and the local TypeScript copies in `batchesApi.ts` / `templatesApi.ts`.

Purpose: This is the schema-first foundation that every downstream plan in Phase 8 depends on. Without these types, neither the backend validation engine (Plan 02) nor the frontend rule editor (Plan 03) nor the badge surfacing (Plan 04) can compile or run. Following the Phase 03.1 `prompt_template` pattern faithfully — JSON Schema → turbo generate → manual schemas.py mirror.
Output: Updated schema files, regenerated codegen artifacts, extended Pydantic models, extended frontend API type copies — all backward-compatible (null defaults so existing batches/templates load unchanged).
</objective>

<execution_context>
@/Users/zu54tav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zu54tav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-validation-rules-engine-per-field-regex-vocabulary-llm-correction-rules-applied-after-vlm-extraction-surfaced-as-field-status-badges-in-results-and-verify-views/08-CONTEXT.md
@.planning/phases/08-validation-rules-engine-per-field-regex-vocabulary-llm-correction-rules-applied-after-vlm-extraction-surfaced-as-field-status-badges-in-results-and-verify-views/08-RESEARCH.md

@packages/shared-types/schemas/template.schema.json
@packages/shared-types/schemas/batch.schema.json
@apps/backend/app/models/schemas.py
@apps/frontend/src/api/batchesApi.ts
@apps/frontend/src/api/templatesApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend JSON schemas with FieldRule + ValidationOutcome and run codegen</name>
  <files>
    packages/shared-types/schemas/template.schema.json
    packages/shared-types/schemas/batch.schema.json
    packages/shared-types/generated/ts/index.ts
    packages/shared-types/generated/py/template.py
    packages/shared-types/generated/py/batch.py
  </files>
  <action>
    Edit `packages/shared-types/schemas/template.schema.json`:
    1. Add a new `FieldRule` definition under `definitions`:
       ```json
       "FieldRule": {
         "type": "object",
         "title": "FieldRule",
         "properties": {
           "preset_id":        { "anyOf": [{"type":"string"}, {"type":"null"}], "default": null },
           "pattern":          { "anyOf": [{"type":"string"}, {"type":"null"}], "default": null,
                                  "description": "Regex pattern. Null means no regex rule." },
           "vocabulary":       { "anyOf": [{"items":{"type":"string"},"type":"array"}, {"type":"null"}], "default": null,
                                  "description": "Closed vocabulary list. Null means no vocab rule." },
           "fuzzy_distance":   { "anyOf": [{"type":"integer"}, {"type":"null"}], "default": null,
                                  "description": "Levenshtein distance for opt-in fuzzy vocab match. Null = exact." },
           "corrector_enabled":{ "type": "boolean", "default": false,
                                  "description": "Per-field opt-in for LLM corrector when rule fails." }
         }
       }
       ```
    2. Add a `field_rules` property to `Template`, `TemplateCreate`, and `TemplateUpdate`:
       ```json
       "field_rules": {
         "anyOf": [
           { "additionalProperties": { "$ref": "#/definitions/FieldRule" }, "type": "object" },
           { "type": "null" }
         ],
         "default": null,
         "title": "Field Rules",
         "description": "Map of field_label -> FieldRule. Null means no validation rules."
       }
       ```

    Edit `packages/shared-types/schemas/batch.schema.json`:
    1. Add a new `ValidationOutcome` definition under `definitions`:
       ```json
       "ValidationOutcome": {
         "type": "object",
         "title": "ValidationOutcome",
         "properties": {
           "status":             { "type":"string", "enum": ["valid","invalid","corrected","skipped"] },
           "rule_failed":        { "anyOf": [{"type":"string"}, {"type":"null"}], "default": null },
           "original_value":     { "anyOf": [{"type":"string"}, {"type":"null"}], "default": null },
           "rationale":          { "anyOf": [{"type":"string"}, {"type":"null"}], "default": null },
           "corrector_proposal": { "anyOf": [{"type":"string"}, {"type":"null"}], "default": null }
         },
         "required": ["status"]
       }
       ```
    2. Add three properties to `BatchConfig` and `BatchCreate`:
       - `field_rules` — same shape as Template.field_rules above (anyOf object-of-FieldRule | null, default null), but the FieldRule `$ref` must be inlined as a `$ref` to the SAME-FILE definition; codegen does not cross-reference between schema files. Inline the FieldRule definition into batch.schema.json as well (duplicate the definition body — Phase 03.1 used cross-file refs poorly so we keep it self-contained).
       - `corrector_enabled` — `{"type": "boolean", "default": false}`
       - `corrector_cap` — `{"anyOf": [{"type":"integer"}, {"type":"null"}], "default": 100}`

       NOTE: If `packages/shared-types/scripts/generate.mjs` already supports cross-file `$ref` resolution (check the script before deciding), keep a single FieldRule definition in template.schema.json and reference it via `{ "$ref": "template.schema.json#/definitions/FieldRule" }`. If unsupported, duplicate the definition into batch.schema.json. Using local refs `#/definitions/FieldRule` AFTER inlining is the safest path. Choose whichever the codegen supports — the goal is a clean regenerate.
    3. Add a `validation` property to `ExtractionResult`:
       ```json
       "validation": {
         "anyOf": [
           { "additionalProperties": { "$ref": "#/definitions/ValidationOutcome" }, "type": "object" },
           { "type": "null" }
         ],
         "default": null,
         "title": "Validation",
         "description": "Per-field validation outcomes. Null means validation not run."
       }
       ```

    All new fields MUST have `default: null` or `default: false`/`default: 100` so existing payloads without these keys are still valid. Do NOT add anything to a `required` array.

    Run codegen from repo root:
    ```bash
    npm run --workspace @indexcards/shared-types generate
    # or, if the project uses turbo:
    npx turbo run generate --filter=@indexcards/shared-types
    ```
    Inspect `packages/shared-types/generated/ts/index.ts` and `packages/shared-types/generated/py/*.py` and confirm `FieldRule`, `ValidationOutcome`, and the new properties on Template/Batch types appear. Do NOT hand-edit these generated files — re-run codegen if output is wrong.

    Common pitfall (from research): if the codegen helper produces `Record<string, FieldRule> | null` differently than expected, the resulting TS types may use intermediate aliases. Inspect the output once and accept whatever the existing pipeline emits — frontend code in later plans will adapt.
  </action>
  <verify>
    <automated>cd /Users/zu54tav/workspace/Indexcards_OCR && npx turbo run generate --filter=@indexcards/shared-types 2>&1 | tail -20 &amp;&amp; grep -q "FieldRule" packages/shared-types/generated/ts/index.ts &amp;&amp; grep -q "ValidationOutcome" packages/shared-types/generated/ts/index.ts &amp;&amp; grep -q "field_rules" packages/shared-types/generated/ts/index.ts &amp;&amp; grep -q "validation" packages/shared-types/generated/ts/index.ts &amp;&amp; echo OK</automated>
    <manual>Open `packages/shared-types/generated/ts/index.ts` and confirm FieldRule, ValidationOutcome interfaces exist and Template/BatchCreate/ExtractionResult include the new optional properties.</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    JSON schema files contain new definitions and properties. `turbo generate` (or equivalent) succeeds without errors. Generated TypeScript file in `packages/shared-types/generated/ts/index.ts` contains `FieldRule`, `ValidationOutcome`, `field_rules`, `validation`, `corrector_enabled`, `corrector_cap` as expected.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mirror schema in Pydantic schemas.py and frontend API type copies</name>
  <files>
    apps/backend/app/models/schemas.py
    apps/frontend/src/api/batchesApi.ts
    apps/frontend/src/api/templatesApi.ts
  </files>
  <action>
    Edit `apps/backend/app/models/schemas.py`:
    1. Add new Pydantic models near the top of the file (after the existing imports):
       ```python
       class FieldRule(BaseModel):
           preset_id: Optional[str] = None
           pattern: Optional[str] = None
           vocabulary: Optional[List[str]] = None
           fuzzy_distance: Optional[int] = None
           corrector_enabled: bool = False

       class ValidationOutcome(BaseModel):
           status: str  # "valid" | "invalid" | "corrected" | "skipped"
           rule_failed: Optional[str] = None
           original_value: Optional[str] = None
           rationale: Optional[str] = None
           corrector_proposal: Optional[str] = None
       ```
    2. Extend `ExtractionResult` to include:
       ```python
       validation: Optional[Dict[str, ValidationOutcome]] = None
       ```
    3. Extend `BatchConfig` to include:
       ```python
       field_rules: Optional[Dict[str, FieldRule]] = None
       corrector_enabled: bool = False
       corrector_cap: Optional[int] = 100
       ```
    4. Extend `BatchCreate` to include the same three fields as `BatchConfig`.
    5. Extend `Template`, `TemplateCreate`, `TemplateUpdate` to include:
       ```python
       field_rules: Optional[Dict[str, FieldRule]] = None
       ```

    All new fields MUST have safe defaults (None / False / 100) so existing payloads are accepted unchanged.

    Edit `apps/frontend/src/api/batchesApi.ts`:
    1. Add interface at top of file (or import from generated types if the file already imports — researcher confirmed it uses LOCAL copies; add inline):
       ```typescript
       export interface FieldRule {
         preset_id?: string | null;
         pattern?: string | null;
         vocabulary?: string[] | null;
         fuzzy_distance?: number | null;
         corrector_enabled?: boolean;
       }
       ```
    2. Extend the local `BatchCreate` interface (around line 6-11) with:
       ```typescript
       field_rules?: Record<string, FieldRule> | null;
       corrector_enabled?: boolean;
       corrector_cap?: number | null;
       ```
    3. Update the `createBatch` function body to pass the new fields through to the POST body (do NOT drop them).
    4. Add a `ValidationOutcome` interface and extend `ExtractionResult` (whatever the local type is called) with `validation?: Record<string, ValidationOutcome> | null`:
       ```typescript
       export interface ValidationOutcome {
         status: 'valid' | 'invalid' | 'corrected' | 'skipped';
         rule_failed?: string | null;
         original_value?: string | null;
         rationale?: string | null;
         corrector_proposal?: string | null;
       }
       ```

    Edit `apps/frontend/src/api/templatesApi.ts`:
    1. Add or import `FieldRule` interface (re-export from batchesApi or duplicate — match existing local-copy style).
    2. Extend the local `Template`, `TemplateCreate`, `TemplateUpdate` interfaces with `field_rules?: Record<string, FieldRule> | null`.
    3. Update mutation/fetch call bodies to include `field_rules` when present.

    Common pitfall (Pitfall 6 in research): batchesApi.ts and templatesApi.ts are NOT auto-synced with codegen. If you skip this step the frontend silently never sends field_rules to the backend.
  </action>
  <verify>
    <automated>cd /Users/zu54tav/workspace/Indexcards_OCR &amp;&amp; python -c "from apps.backend.app.models.schemas import FieldRule, ValidationOutcome, BatchCreate, ExtractionResult, Template; b = BatchCreate(custom_name='x', session_id='y'); assert b.field_rules is None and b.corrector_enabled is False and b.corrector_cap == 100; r = ExtractionResult(filename='f', batch='b', success=True, duration=0.0); assert r.validation is None; t = Template(id='1', name='x', fields=[]); assert t.field_rules is None; print('OK')" &amp;&amp; cd apps/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -20</automated>
    <manual>Confirm Pydantic models load without errors, frontend tsc has no NEW errors related to the new types (existing unrelated errors are fine).</manual>
    <sampling_rate>run after this task commits, before next phase begins</sampling_rate>
  </verify>
  <done>
    `apps/backend/app/models/schemas.py` exposes FieldRule, ValidationOutcome, and the extended Batch/Template/ExtractionResult models with backward-compatible defaults. Frontend `batchesApi.ts` and `templatesApi.ts` carry the new optional fields in their local interface copies and pass them through to API calls. Python smoke test passes; TypeScript compiles without new type errors related to these additions.
  </done>
</task>

</tasks>

<verification>
- Schema JSON files updated with FieldRule, ValidationOutcome, field_rules, corrector_enabled, corrector_cap, validation.
- Codegen executed; generated TS file contains all new types.
- `apps/backend/app/models/schemas.py` mirrors the schema additions with correct Pydantic types and null/false/100 defaults.
- `batchesApi.ts` and `templatesApi.ts` local interface copies extended.
- Existing API contract unchanged: a payload with no field_rules / no validation still validates (backward compatibility).
</verification>

<success_criteria>
- Pydantic smoke test from Task 2 verify command exits 0.
- TypeScript codegen + tsc produces no NEW errors related to the additions.
- An existing batch's `config.json` (without `field_rules` or `corrector_enabled` keys) loads through the new BatchConfig model without raising.
</success_criteria>

<output>
After completion, create `.planning/phases/08-validation-rules-engine-per-field-regex-vocabulary-llm-correction-rules-applied-after-vlm-extraction-surfaced-as-field-status-badges-in-results-and-verify-views/08-01-SUMMARY.md`
</output>
